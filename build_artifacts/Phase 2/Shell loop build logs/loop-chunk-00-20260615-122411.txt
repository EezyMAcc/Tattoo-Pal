Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Chunk 0 is complete and committed. Here's a summary of what was done:

**Chunk 0 — done and committed (`8d2ace7`)**

- Confirmed the branch is `feat/remote-app` with no prior phase-2 chunk commits.
- Ran the full gate before touching anything: 102 tests, 100% coverage, mypy strict clean, ruff clean — green.
- Inspected `mcp==1.27.2` directly and verified all required APIs are present: `AuthSettings`, `TokenVerifier`, `AccessToken`, `FastMCP(auth=, token_verifier=)`, and `Resource(mimeType="text/html;profile=mcp-app")`. **No version bump needed.**
- Key finding documented: the SDK's `RequireAuthMiddleware` enforces `required_scopes` automatically (403 path) — Chunk 2's verifier only needs to validate JWT signature/iss/aud/exp and populate `AccessToken.scopes`.
- Wrote `RECONCILIATION.md` at the repo root with all findings so later chunks don't re-derive them.
- Gate re-run after adding the file: still 100% green. Committed.
